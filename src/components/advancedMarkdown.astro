---
const html = await Astro.slots.render("default");
const regex = /id=/g;
const fixed = html.replace(regex, 'class="Advanced-class" id=');
---

<div class="Advanced-class" style="display: none;" ;><slot /></div>
